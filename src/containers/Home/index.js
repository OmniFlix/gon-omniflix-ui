import React from 'react';
import './index.css';
import NavBar from '../NavBar';

const Home = () => {
    return (
        <div className="home">
            <NavBar />
        </div>
    );
};

export default Home;
